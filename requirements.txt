# 依赖后面注明哪个脚本需要，方便删脚本时对照清理

tqdm>=4.66               # 所有脚本：进度条

h5py>=3.10               # download_mur_sst.py：校验下载的文件是否被截断，必需

copernicusmarine>=2.4.1  # download_glorys.py / download_duacs.py: CMEMS toolbox
netCDF4>=1.6            # download_duacs.py: NetCDF validation and provenance
numpy>=1.26            # download_duacs.py: coordinate validation
xarray>=2024.10          # download_glorys.py / download_duacs.py: NetCDF validation

# cdsapi>=0.7            # download_era5.py
